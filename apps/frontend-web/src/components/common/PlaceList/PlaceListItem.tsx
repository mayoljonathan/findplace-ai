import React from "react";

interface PlaceListItemProps {}

export const PlaceListItem: React.FC<PlaceListItemProps> = (props) => {
  const {} = props;

  return <div>PlaceListItem</div>;
};

PlaceListItem.displayName = "PlaceListItem";
